<UserControl x:Class="BudgetTracker.Views.TransactionForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BudgetTracker.ViewModels"
             Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="DescriptionTextBox" Margin="5" PlaceholderText="Description"/>
        <TextBox x:Name="AmountTextBox" Grid.Row="1" Margin="5" PlaceholderText="Amount"/>
        <ComboBox x:Name="TypeComboBox" Grid.Row="2" Margin="5" >
            <ComboBoxItem Content="Income" />
            <ComboBoxItem Content="Expense" />
        </ComboBox>
        <Button Content="Save" Grid.Row="3" Margin="5" Width="100" HorizontalAlignment="Left"/>
    </Grid>
</UserControl>
